Echo State Network :class:`reservoirpy.ESN`
===========================================

.. currentmodule:: reservoirpy

.. autoclass:: ESN

   .. rubric:: Methods

   .. autosummary::

      ~ESN.initialize
      ~ESN.step
      ~ESN.run
      ~ESN.predict
      ~ESN.fit
      ~ESN.partial_fit


   .. rubric:: Attributes

   .. autosummary::

    ~ESN.reservoir
    ~ESN.readout
    ~ESN.feedback
    ~ESN.input_to_readout
    ~ESN.input_node
    ~ESN.nodes
    ~ESN.edges
    ~ESN.inputs
    ~ESN.outputs
    ~ESN.named_nodes
    ~ESN.trainable_nodes
    ~ESN.execution_order
    ~ESN.parents
    ~ESN.children
    ~ESN.is_trainable
    ~ESN.is_multi_input
    ~ESN.is_multi_output
    ~ESN.is_parallel
    ~ESN.initialized